export const metadata = {
	title: {
		default: 'Projects',
		template: '%s | Projects | Zyzle.dev',
	},
};

export default function ProjectsLayout({ children }: { children: React.ReactNode }) {
	return <>{children}</>;
}
