export const metadata = {
	title: {
		default: 'Tags',
		template: '%s | Tags | Zyzle.dev',
	},
};

export default function TagsLayout({ children }: { children: React.ReactNode }) {
	return <>{children}</>;
}
